[Login]
# Close the Pixelbook lid into suspend, then hibernate after the configured
# delay. Apply the same policy on AC power; leave the power key untouched.
HandleLidSwitch=suspend-then-hibernate
HandleLidSwitchExternalPower=suspend-then-hibernate
HandleLidSwitchDocked=suspend-then-hibernate
