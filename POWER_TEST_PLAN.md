# Pixelbook Go 电源与睡眠测试计划

注意：本轮只做只读检查，没有执行挂起、睡眠、唤醒、重启、关机、合盖、拔电或改变电源配置的操作。

## 已完成的安全检查

- `systemctl is-system-running`：`running`。
- `systemctl --failed`：0 个 failed units。
- `/sys/power/mem_sleep`：支持 `s2idle` 和 `deep`，当前默认选择 `s2idle`（方括号表示当前选择）。
- `/sys/power/state`：支持 `freeze mem disk`；只是读取，没有写入。
- 电池、USB-PD 充电器、ChromeOS EC、温度区域和 `intel_pstate` 均已枚举。
- 当前温度基线约为：CPU 封装 60°C、PCH 50.5°C、Wi‑Fi 55°C、电池 38.9°C。
- lid/ACPI 电源接口存在；没有读取到异常的 systemd unit。

## 还需要实际测试的项目

| 项目 | 测试动作 | 风险/前提 |
|---|---|---|
| 睡眠/唤醒 | 保存工作后执行一次 `systemctl suspend`，等待 30–60 秒，再按电源键唤醒 | 可能唤醒失败或丢失当前会话；需用户明确允许 |
| 深度睡眠 | 临时选择 `deep` 后重复一次睡眠/唤醒 | 可能比 s2idle 更容易出现固件/驱动问题；测试后要恢复原选择 |
| 合盖/开盖 | 合盖 30 秒、开盖，检查显示、Wi‑Fi、音频、键盘和触摸板 | 会改变睡眠状态；不要在未保存工作时测试 |
| 外接电源 | 记录 AC online、拔插 USB-C 电源，再检查充电状态和电池电流 | 当前电脑可能立即转为放电；需接近充电器并保存工作 |
| 电池续航 | 100% 充电后记录 30/60 分钟空闲和轻负载电量变化 | 需要较长时间，不能仅靠一次命令判断健康度 |
| 热管理 | 轻负载/浏览器/视频播放时记录温度、频率、风扇和 throttling | 不需要改变配置，可后续做 |
| USB-C/Thunderbolt | 插拔充电器、U 盘、显示器或扩展坞，检查 `boltctl`、USB、显示和充电 | 需要实际外设；不建议盲目运行 AER 的 `setpci` 服务 |
| 断电恢复 | 仅在有备份且不需要使用电脑时，测试异常断电后的文件系统/启动 | 高风险，本轮不做 |

## 建议顺序

先做“外接电源状态读取 → 轻负载温度 → 普通 s2idle 睡眠/唤醒 → 合盖 → deep 睡眠”。每次只改变一个变量，并在测试前保存 `journalctl -b -k` 和 `systemd-analyze dump`。如果睡眠测试出现黑屏、无法唤醒或音频/触控失效，下一步先收集日志，不要立即添加内核参数。
